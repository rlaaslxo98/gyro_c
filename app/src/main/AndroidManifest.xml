<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dudwo.gyrocounter">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BaseActivity" />
        <activity android:name=".EmailPasswordActivity" />
        <activity android:name=".AddWork" />
        <activity
            android:name=".DeviceListActivity"
            android:theme="@android:style/Theme.DeviceDefault.Dialog" />
        <activity android:name=".MeasureValue" />
        <activity android:name=".RunActivity" />
        <activity
            android:name=".SetRunActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Dialog" />
        <activity android:name=".IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FinishRunActivity" />
        <activity android:name=".CountRunActivity" />
        <activity android:name=".WhileRunActivity" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".Help" />
        <activity android:name=".result_db"></activity>
    </application>

</manifest>